name: flutter_split
description: Unofficial Flutter plugin as a simple wrapper of Split.io API.
version: 0.0.2+1
homepage: https://github.com/gonzalonm/flutter_split

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.4

dev_dependencies:
  flutter_lints: ^1.0.0

assets:
  - assets/