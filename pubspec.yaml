name: flutter_split
description: Split API implementation for Flutter.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.4

dev_dependencies:
  flutter_lints: ^1.0.0